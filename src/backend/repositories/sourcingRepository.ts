// Kept for reference — this thin interface is superseded by SourcingEventRepository (M4).
// SourcingEventService uses SourcingEventRepository directly.
export type { SourcingEventRepository } from './sourcingEventRepository';
